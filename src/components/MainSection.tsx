'use client'

import styled from "styled-components"

export default function MainSection(){
    return(
        <main>
            <P>메인이다</P>
        </main>
    )
}

const P = styled.p`
    color: red;
`